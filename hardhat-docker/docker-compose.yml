services:
  hardhat-test:
    build: .
    volumes:
      - ../dataset/vulnerable_contracts:/tmp/vulnerable_contracts
      - ../dataset/mal_contracts:/tmp/mal_contracts
      - ../dataset/tests:/tmp/tests
    working_dir: /usr/src/app
